<!--{ ids:[181], language:'JavaScript', type:'workshop', order: 0, name:'A Simple Array', description:'An array is a list of data' } -->
var expect = require("chai").expect;

describe("fruit1", function() {
  it("is assigned the correct value", function() {
    expect(fruit1).to.eql("apple");
  });
});
describe("fruit2", function() {
  it("is assigned the correct value", function() {
    expect(fruit2).to.eql("banana");
  });
});
describe("fruit3", function() {
  it("is assigned the correct value", function() {
    expect(fruit3).to.eql("orange");
  });
});
