var expect = require("chai").expect;

describe("color", function() {
  it("has a value of \"orange\"", function() {
    expect(color).to.eql("orange");
  });
});
describe("shape", function() {
  it("has a value of \"round\"", function() {
    expect(shape).to.eql("round");
  });
});
