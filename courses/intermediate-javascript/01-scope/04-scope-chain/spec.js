var expect = require("chai").expect;

describe("goldenGateParkReturns", function() {
  it("is assigned the number corresponding to the correct array", function() {
    expect(goldenGateParkReturns).to.eql(3);
  });
});
