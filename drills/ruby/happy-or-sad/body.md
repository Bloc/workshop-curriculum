Write a program with a `smiley`method that accepts a hash as an argument.

If the hash element with the key "mood" has the value "happy", return `":)"`

If it has the value "sad", return `":("`

Otherwise, return `":|"`
