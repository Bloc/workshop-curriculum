Create a program with a `smoosh` method that takes two strings as arguments and returns the two of them "smooshed" together. For example:

```
smoosh("chocolate", "chips") #=> should return "chocolatechips"
```
